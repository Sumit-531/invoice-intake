"""The human queue: what could not be automated, and why.

NOT BUILT YET. Today a blocked invoice prints its findings and exits non-zero; the queue
that collects them into a file a person works from is the next slice.

The package exists now rather than later because CLAUDE.md's architecture names it, and a
contract test asserts that the tree and the contract agree. An empty package that says
what is missing is honest. A missing package that nothing mentions is how a plan quietly
loses a layer.
"""
